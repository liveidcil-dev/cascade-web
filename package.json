{
  "name": "cascade-management-site",
  "version": "1.0.0",
  "private": true,
  "description": "Modern local website rebuild for Cascade Management.",
  "scripts": {
    "build": "node -e \"console.log('No build step required. Cloudflare Pages output directory: public')\"",
    "dev": "node server.js",
    "start": "node server.js"
  }
}
